"""Shared infrastructure layer for the vision engine.

- ``renderer`` — PDF → PNG rendering, adaptive DPI, 2x2 tiling.
- ``prompts`` — markdown prompt loading with frontmatter + templating.
- ``invoker`` — provider dispatch for a single forced tool call.
"""
