"""Vision engine.

Submodules:

- ``core``               — shared infrastructure: renderer, prompt loader,
  provider-dispatched tool invoker.
- ``rfi``                — RFI letter extractor.
- ``value_engineering``  — VE analyser (cost-reduction opportunities).
- ``plans``              — plan-analyser vision passes (full plan-analyser
  orchestration lives in ``app.plans``).
"""
